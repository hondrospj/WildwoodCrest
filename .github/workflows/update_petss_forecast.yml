

name: Update PETSS Forecast

on:
  workflow_dispatch:
  schedule:
    # Every 3 hours (UTC)
    - cron: "15 */3 * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: update-petss-forecast
  cancel-in-progress: true

jobs:
  update-petss:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run PETSS forecast updater
        env:
          PETSS_STID: "8535901"   # station
          PETSS_DATUM: "MLLW"     # metadata only
        run: |
          set -euo pipefail
          echo "Running PETSS forecast script…"
          echo "STID=$PETSS_STID"
          echo "DATUM=$PETSS_DATUM"
          node -v
          pwd
          node tools/update_petss_forecast.js

      - name: Commit & push if changed
        id: forecast_commit
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Commit only the forecast products (+ debug header snapshot, optional)
          git add data/petss_forecast.csv data/petss_forecast.json data/petss_meta.json data/petss_station_debug.txt || true

          if git diff --cached --quiet; then
            echo "No changes to commit."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git commit -m "Update PETSS forecast"
          git pull --rebase --autostash origin main
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Configure Pages
        if: steps.forecast_commit.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        if: steps.forecast_commit.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: actions/upload-pages-artifact@v5
        with:
          path: .

      - name: Deploy Pages
        id: deployment
        if: steps.forecast_commit.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: actions/deploy-pages@v5
